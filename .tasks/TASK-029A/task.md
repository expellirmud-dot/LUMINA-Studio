# TASK-029A: Update LUMINA Controller and CLI Routing Policy

## Objective
Update LUMINA agent/model routing documentation so the project clearly supports both Codex GPT-5.4 as primary Controller and AntiGravity / AGY Gemini 3.1 Pro Low/High as fallback Controller. Also clarify Controller-specific CLI routing.

## Required Updates
1. `AGENTS.md`: Add "Goal Execution Contract" section.
2. `AGENTS.md`: Add Controller-specific CLI routing.
3. `docs/LUMINA_MODEL_AND_WORKER_POLICY.md`: Update model routing.
4. Validation via `git diff --check` and `git status --short`.
