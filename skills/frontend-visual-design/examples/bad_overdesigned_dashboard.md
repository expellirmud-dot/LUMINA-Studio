# Bad Overdesigned Dashboard Shell

Avoid a dashboard that:

- buries task state inside decorative hero art
- mixes commands, reports, and logs into one visual blob
- uses animated gradients as the main hierarchy cue
- makes the primary action less obvious than the background styling
- hides blockers behind visual polish
