# Bad Mixed-Purpose Token Soup

Avoid tokens that:

- mix semantic meaning with one-off component names
- redefine the same color for every panel
- use slightly different grays for every card and border
- make focus, disabled, warning, and error states look unrelated
- force component-level overrides for basic consistency
